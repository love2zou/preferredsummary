# reservation-ui 接口联调测试流程表

| 序号 | 测试功能点 | 验证状态 | 备注 |
|---|---|---|---|
| 1 | 初始化演示数据 `POST /api/ReservationDemo/seed` | 已验证 | 用于生成会员、教练、课包、预约、订单等联调数据 |
| 2 | 会员账号登录页 `Login.vue` | 已验证 | 账号 `app_member_01 / Test123456` 可成功登录 |
| 3 | 教练账号登录页 `Login.vue` | 已验证 | 账号 `app_coach_01 / Test123456` 可成功登录 |
| 4 | 登录后路由分流 | 已验证 | 会员跳 `/home`，教练跳 `/coach` |
| 5 | 首页数据加载 `Home.vue` | 已验证 | 已成功获取 `ReservationApp/home`，会员信息与推荐教练返回正常 |
| 6 | 教练列表页 `TrainerList.vue` | 已验证 | 已成功获取 `ReservationApp/trainers` |
| 7 | 教练详情页 `TrainerDetail.vue` | 待页面验证 | 接口已接通，建议验证页面展示字段是否完整 |
| 8 | 预约页加载 `BookingCoach.vue` | 已验证 | 已成功获取 `ReservationApp/trainers/{id}/booking`，课时与可预约时间正常返回 |
| 9 | 提交预约 | 已验证 | 已成功调用 `POST /api/ReservationApp/reservations` 创建预约 |
| 10 | 预约成功后跳转预约流程页 | 已验证 | 已带 `reservationId` 跳转到 `ReservationFlow.vue` |
| 11 | 我的预约列表 `MyReservations.vue` | 已验证 | 已成功获取 `upcoming/completed/cancelled` 分类数据 |
| 12 | 从预约列表进入取消预约流程 | 已验证 | 当前预约 `reservationId` 已正确透传到流程页 |
| 13 | 取消预约提交 | 已验证 | 已成功调用 `POST /api/ReservationApp/reservations/cancel` |
| 14 | 取消预约后刷新状态 | 已验证 | 取消后预约状态变化正常，课时已返还 |
| 15 | 预约流程页-预约成功场景 | 待页面验证 | 页面已接真实数据，建议检查文案、时间、教练、门店展示 |
| 16 | 预约流程页-取消预约场景 | 待页面验证 | 已接真实取消动作，建议点击验收完整交互 |
| 17 | 预约流程页-签到/评价/联系教练场景 | 待页面验证 | 当前为展示型流程页，数据来源真实，但签到/评价未做真实提交 |
| 18 | 我的课程页 `TrainingHub.vue` | 已验证 | 已改为读取真实 `profile + home + commerce` 数据 |
| 19 | 课程订单信息展示 | 待页面验证 | 数据来自 `ReservationPortal/member/commerce`，建议检查状态文案与跳转 |
| 20 | 到期提醒/优惠券提醒展示 | 待页面验证 | 当前由真实课包、优惠券、下一节预约数据组合生成 |
| 21 | 个人中心 `Profile.vue` | 待页面验证 | 建议验证会员课包、剩余课时、有效期展示是否符合预期 |
| 22 | 会员中心 `MemberCenter.vue` | 待页面验证 | 页面已走真实接口，建议检查体测、训练记录、通知展示 |
| 23 | 商城中心 `CommerceCenter.vue` | 待页面验证 | 页面已走真实接口，建议检查课包、订单、优惠券三个场景 |
| 24 | 教练工作台 `CoachDashboard.vue` | 已验证 | 已成功获取 `ReservationPortal/coach/workbench`，成员数与待审核数正常 |
| 25 | 教练工作台-排班/成员/审核场景 | 待页面验证 | 接口已通，建议逐页点击确认展示与切换逻辑 |
| 26 | 鉴权失效处理 | 待页面验证 | 建议手动清空 token 后访问会员页/教练页，检查是否跳回登录页 |
| 27 | 接口异常可见性 | 已验证 | 会员预约主链路已去掉 mock 静默回退，接口失败会真实暴露 |
| 28 | 前端构建 | 已验证 | `npm run build` 已通过 |
| 29 | 后端构建 | 已验证 | `dotnet build` 已通过 |
| 30 | 整体会员主链路 | 已验证 | 已跑通：seed -> 登录 -> 首页 -> 预约页 -> 创建预约 -> 我的预约 -> 取消预约 |

